// "use client";
import React from 'react';
import styles from './LazyWaiting.module.scss';

export interface LazyWaitingProps {
	// types...
}

const LazyWaiting: React.FC<LazyWaitingProps>  = ({}) => {
	return (
		<div className={styles.lazywaiting}>
 		</div>
	);
};

export default LazyWaiting;
